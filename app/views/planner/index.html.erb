<h1>Welcome to the Planner on Rails App!</h1>

<body>
  <% if user_signed_in? %>
  	<h2><%= current_user.email %>'s months:</h2>
  		<% @months.each do |month| %>
  			<li><%= link_to month.month_name, month_path(month) %></li>
		<% end %><br>
  <% end %>
</body>